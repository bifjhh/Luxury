<view class="kd_list">
  <view class="list {{item==0?'one':''}}" wx:for="{{info}}" wx:key="i">
  <text class="info">{{item.AcceptStation}}</text>
  <text class="time">{{item.AcceptTime}}</text>
</view>
<view class="xian"></view>
</view>